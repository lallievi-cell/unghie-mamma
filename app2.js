function card(a){const c=C(a.clientId),s=S(a.serviceId),due=(+a.price||0)-(+a.paid||0);const st=a.status==="done"?"Fatta":a.status==="cancelled"?"Annullata":"Prenotata";return `<div class="card" onclick="openApt('${a.id}')"><div class="apt"><div class="timebox">${esc((a.time||"--:--").slice(0,5))}<small>${s?s.minutes+" min":""}</small></div><div style="flex:1"><div class="name">${esc(c?c.name:"Cliente")}</div><div class="muted">${esc(s?s.name:"Servizio")}</div><div style="margin-top:8px;display:flex;gap:6px;flex-wrap:wrap"><span class="chip">${st}</span><span class="chip ${due>0.01?"debt":"paid"}">${due>0.01?"Da pagare "+euro(due):euro(a.price||0)}</span></div></div></div></div>`}
function vToday(){const list=apts(today()).filter(a=>a.status!=="cancelled");if(!list.length)return `<div class="card empty"><div class="big">💅</div><p>Nessun appuntamento oggi.</p><p class="tiny" style="margin-top:8px">Tocca + per aggiungerne uno.</p></div>`;const next=list.filter(a=>a.status==="booked"),done=list.filter(a=>a.status==="done");return next.map(card).join("")+(done.length?'<p class="muted" style="margin:8px 4px">Già fatte</p>'+done.map(card).join(""):"")}
function vAgenda(){const[y,m]=selectedDate.split("-").map(Number);const first=new Date(y,m-1,1),start=(first.getDay()+6)%7,dim=new Date(y,m,0).getDate();let cells="";for(let i=0;i<start;i++)cells+="<div></div>";for(let d=1;d<=dim;d++){const iso=y+"-"+String(m).padStart(2,"0")+"-"+String(d).padStart(2,"0");const cls=["day",iso===selectedDate?"sel":"",apts(iso).some(a=>a.status!=="cancelled")?"has":"",iso===today()?"today":""].join(" ");cells+=`<button type="button" class="${cls}" onclick="selectedDate='${iso}';render()">${d}</button>`}const list=apts(selectedDate);return `<div class="card"><div class="row" style="margin-bottom:10px"><button type="button" class="btn btn-ghost btn-sm" onclick="shiftM(-1)">←</button><strong style="text-transform:capitalize">${first.toLocaleDateString("it-IT",{month:"long",year:"numeric"})}</strong><button type="button" class="btn btn-ghost btn-sm" onclick="shiftM(1)">→</button></div><div class="cal-grid">${["L","M","M","G","V","S","D"].map(x=>`<div class="dow">${x}</div>`).join("")}${cells}</div></div><p class="muted" style="margin:4px 4px 10px;text-transform:capitalize">${ndl(selectedDate)}</p>`+(list.length?list.map(card).join(""):`<div class="card empty"><p>Nessun appuntamento.</p></div>`)}
function shiftM(k){const[y,m]=selectedDate.split("-").map(Number);const dt=new Date(y,m-1+k,1);selectedDate=dt.getFullYear()+"-"+String(dt.getMonth()+1).padStart(2,"0")+"-01";render()}
function vClients(){const list=db.clients.filter(c=>!q||(c.name||"").toLowerCase().includes(q)||(c.phone||"").includes(q)).sort((a,b)=>a.name.localeCompare(b.name,"it"));if(!list.length)return `<div class="card empty"><div class="big">👩</div><p>${db.clients.length?"Nessun risultato.":"Nessuna cliente ancora."}</p></div>`;return list.map(c=>{const b=bal(c.id),lv=last(c.id);return `<div class="card" onclick="openClient('${c.id}')"><div class="row"><div><div class="name">${esc(c.name)}</div><div class="muted">${esc(c.phone||"Nessun telefono")} · ${lv?"ultima "+nd(lv.date):"mai venuta"}</div></div>${b>0.01?`<span class="chip debt">${euro(b)}</span>`:""}</div>${c.allergies?`<div class="tiny" style="margin-top:8px;color:var(--bad)">⚠ ${esc(c.allergies)}</div>`:""}</div>`}).join("")}
function vMoney(){const mo=today().slice(0,7),done=db.appointments.filter(a=>a.status==="done");const md=done.filter(a=>(a.date||"").startsWith(mo));const inc=md.reduce((s,a)=>s+(+a.paid||0),0);const cred=db.clients.map(c=>({c,b:bal(c.id)})).filter(x=>x.b>0.01);return `<div class="card"><div class="muted">Incassato questo mese</div><div class="money" style="font-size:32px;margin:4px 0">${euro(inc)}</div></div><div class="card"><h3>Chi deve ancora pagare</h3>${cred.length?cred.map(x=>`<div class="list-item row" onclick="openClient('${x.c.id}')"><div class="name">${esc(x.c.name)}</div><span class="chip debt">${euro(x.b)}</span></div>`).join(""):"<p class=\"muted\" style=\"margin-top:8px\">Nessun credito aperto.</p>"}</div><div class="card"><h3>Backup</h3><div class="grid2"><button type="button" class="btn btn-soft" onclick="exp()">Salva copia</button><button type="button" class="btn btn-ghost" onclick="document.getElementById('imp').click()">Ripristina</button></div><input id="imp" type="file" accept="application/json" class="hidden" onchange="imp(event)"></div>`}
